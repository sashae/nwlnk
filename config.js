// basic server config
var server = 'irc.subtle.org';
var port = '6697';
var secure = 'true';
//allows for self-signed or non-CA approved certs, like letsencrypt.
var selfSigned = 'true';
var certExpired = 'false';

// where are we going
var nick = 'nwlnk';
var realName = 'the shortest of links';
var channels = ['#somewhere, #bottest'];

// probably want to turn this off once running for real
var debug = 'true';
var showErrors = 'true';

// shortener config
var shortenHost = 'https://pab.st';
